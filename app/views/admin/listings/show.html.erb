<h2>Showing Listing: <%= @listing.street_address %></h2>
<p class="show">More info will be added later.</p>
<%= debug @listing %>
<h4>Created at: <%= @listing.created_at.strftime("%Y-%m-%d %H:%M:%S") %></h4>
<h4>Last Updated at: <%= @listing.updated_at.strftime("%Y-%m-%d %H:%M:%S") %></h4>
<p class="show">
	<%= link_to("Edit Listing", edit_admin_listing_path(@listing)) %> |
	<%= link_to("Delete Listing", admin_listing_path(@listing), :method => :delete, :confirm => "Are you sure you want to delete this listing? This action cannot be undone!") %>
</p>
